define(['../../library/ramda.min', './gw_record_fields'], (
  ramda,
  fieldConfig
) => {
  class Transaction {
    constructor() {}
    getTransactions(searchParams) {}
  }

  return new Transaction()
})
